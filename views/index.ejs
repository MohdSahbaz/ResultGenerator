<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SyResult Page</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f8f8f8;
      }
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        text-align: center;
        color: maroon;
        background-color: aquamarine;
        padding: 20px 0;
        margin-bottom: 20px;
        border-radius: 8px;
      }
      .header h1 {
        margin: 0;
      }
      .header p {
        margin: 5px 0 0;
      }
      .newResult {
        text-align: center;
        margin-bottom: 20px;
      }
      .newResult a {
        text-decoration: none;
        color: #333;
        background-color: #ffdab9;
        border: 1px solid #333;
        padding: 10px 20px;
        border-radius: 4px;
        transition: background-color 0.3s ease;
      }
      .newResult a:hover {
        background-color: #ffb6c1;
      }
      .result {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 20px;
      }
      .one {
        border: 1px solid #ccc;
        padding: 20px;
        background-color: #fff;
        border-radius: 8px;
      }
      .one h4 {
        margin-top: 0;
      }
      .one p {
        margin: 5px 0;
      }
      .one a {
        display: block;
        text-align: center;
        text-decoration: none;
        color: #fff;
        background-color: #4caf50;
        padding: 10px;
        border-radius: 4px;
        transition: background-color 0.3s ease;
      }
      .one a:hover {
        background-color: #45a049;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Solker College</h1>
        <p>Solker College of IT</p>
      </div>
      <div class="newResult">
        <a href="/newResult">Add New Result</a>
      </div>
      <h3>Result of IT-Students</h3>
      <div class="result">
        <% for(let stInfo of SyResultData) { %>
        <div class="one">
          <h4>Name: <%= stInfo.name %></h4>
          <p>Roll No: <%= stInfo.rn %></p>
          <a href="./oneResult/<%= stInfo._id %>">Check Result</a>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
